This is where my custom zsh configurations begin

unalias command in aliases.zsh may break some stuff

Bundled oh-my-zsh plugins are used when possible

These are supposed to be git repo's, but i've deleted their git files because i don't need the updates and it messes with my dotfiles repo (nested git repos):
- themes/powerlevel10k/
- plugins/fast-syntax-highlighting/
- plugins/zsh-autosuggestions/

z (zsh-z) stores frequency history in ~/.z
